class Solution {
    public int[] shuffle(int[] nums, int n) {
        int i=0, j=n;
        int[] arr = new int[2*n];
        for(int k=0;k<(2*n);k+=2, i++, j++){
            arr[k] = nums[i];
            arr[k+1] = nums[j];
        }
        return arr;
    }
}
